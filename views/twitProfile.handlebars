{{> header}}
	<div class="container">
		<div class="row-fluid">
			<h3> Hello, {{userProfile.displayName}}! You are currently signed in as {{userProfile.username}} on {{userProfile.provider}}! </h3>
		</div>
		<div class="row-fluid">
			<img class="img-responsive img-circle" src="{{userProfile.photos}}">
		</div>
		pictureUrl = {{userProfile.photos.value}}

	</div>

{{> footer}}